checks:
    javascript: true

filter:
    excluded_paths:
        - node_modules/*
        - coverage/*

        tests:
            override:
                -
                    # command: 'istanbul cover --report clover test.js'
                    command: 'make test'
                    # command: 'npm run test-scrutinizer'
